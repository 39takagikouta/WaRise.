<div class="container mx-auto p-6">
  
<%= form_with model: @form, url: comedy_preferences_path do |f| %>
  # User情報の入力
  <%= f.hidden_field :user_id, value: current_user.id %>
  
  <%= f.label :min_video_length %>
  <%= f.number_field :min_video_length %>

  <%= f.label :max_video_length %>
  <%= f.number_field :max_video_length %>

  # ComedyTags情報の入力
  <% @comedy_tags.each do |tag| %>
    <%= check_box_tag 'comedy_preference_form[comedy_tag_ids][]', tag.id, @form.comedy_tag_ids&.include?(tag.id.to_s) %>
    <%= tag.name %>
  <% end %>

  # Keywords情報の入力
  <%= f.label :keyword_names, "キーワード (,区切り)" %>
  <%= f.text_field :keyword_names %>

  <%= f.submit '登録' %>
<% end %>
  <h1 class="mb-6">お笑い嗜好性入力</h1>

  <div class="mb-4 items-center justify-between">
    <h2>タグ（5つ以上登録してください）</h2>
    <div>
      ここにタグめっちゃ表示する
    </div> 
  </div>

  <div class="mb-4 items-center justify-between">
    <h2>自由記述（タグにあるもの以外で好きなキーワードを、で区切って入力してください）</h2>
    <div>
      キーホルダーめっちゃつけたいな
    </div> 
  </div>

  <div class="mb-4 items-center justify-between">
    <h2>動画の目安時間</h2>
    <div>
      時間を入力
    </div> 
  </div>
</div>