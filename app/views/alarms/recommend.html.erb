<div class="container mx-auto p-6">
  <h1 class="alert-info">動画視聴</h1>
  <div>
    <iframe width="560" height="315" src="https://www.youtube.com/embed/<%= @item.id.video_id %>" frameborder="0" allowfullscreen></iframe>
  </div>
    
  <div class="flex my-4">
    <%= link_to "https://twitter.com/intent/tweet?url=https://wa-rise.com&text=今日は#{@alarm.wake_up_time.strftime('%H:%M')}にこの動画で起床しました！%0Ahttps://www.youtube.com/watch?v=#{ @item.id.video_id }%0A%0Aあなたもわらいずで楽しい朝を迎えましょう！", target: '_blank', class: "inline-block bg-blue-500 text-white px-4 py-3 mr-4 rounded hover:bg-blue-600" do %>
      <p><i class="fa-brands fa-x-twitter"></i>でシェアする</p>
    <% end %>
    <%= button_to '他の動画を見る', viewed_videos_path(video_id: @item.id.video_id), { class: "btn btn-warning", params: { redirect_to: 'recommend' } } %>
  </div>
  <%= form_with(model:@alarm, url: viewed_videos_path(video_id: @item.id.video_id, redirect_to: 'mypage', alarm_id: @alarm.id), method: :post ) do |f| %>
    <div class="mb-4">
      <%= f.label :comment, "コメントを残す", class: "font-bold" %>
      <%= f.text_area :comment, class: "mt-1 block w-full rounded-md border-gray-300 shadow-sm", rows: 3 %>
    </div>
    <div class="mb-4">
      <%= f.submit '視聴完了しマイページに戻る', class: "btn btn-warning"%>
    </div>
  <% end %>
</div>