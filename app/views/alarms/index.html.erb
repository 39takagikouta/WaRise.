<% content_for(:title, "みんなの起床情報") %>
<div class="container mx-auto pb-10">
  <h1 class="my-8 md:my-10 text-center">みんなの起床成功情報</h1>
  
  <div class="grid grid-cols-1 md:grid-cols-3 gap-4">
    <%= render @alarms %>
  </div>

  <%= paginate @alarms %>
</div>
