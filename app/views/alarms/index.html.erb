<div class="container mx-auto p-6">
  <h1 class="mb-6">アラーム一覧</h1>
  
  <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
    <% @alarms.each do |alarm| %>
      <%= render @alarms %>
    <% end %>
  </div>
</div>