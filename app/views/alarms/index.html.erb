<div class="container mx-auto p-6">
  <h1 class="mb-6">みんなの起床情報</h1>
  
  <div class="grid grid-cols-3 gap-4">
    <% @alarms.each do |alarm| %>
      <%= render 'alarm', alarm: alarm %>
    <% end %>
  </div>
</div>