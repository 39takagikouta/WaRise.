<div class="container mx-auto p-6">
  <h1 class="my-8 md:my-14 text-center">みんなの起床成功情報</h1>
  
  <div class="grid grid-cols-1 md:grid-cols-3 gap-4">
    <%= render @alarms %>
  </div>

  <%= paginate @alarms %>
</div>
