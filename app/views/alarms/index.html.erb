<% content_for(:title, "視聴済み動画一覧") %>
<div class="container mx-auto pb-10">
  <h1 class="my-8 md:my-10 text-center">視聴済み動画一覧</h1>
  
  <div class="grid grid-cols-1 md:grid-cols-3 gap-4">
    <%= render @alarms %>
  </div>

  <%= paginate @alarms %>
</div>
